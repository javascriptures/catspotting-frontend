import React from 'react';
const CommentForm = () => {
    //empty form that is called after CommentTemplate mapping
    //allows user to create and post a new comment
    //adds a new comment to the Comment API
    //has input fields and buttons from Material UI
    //input fields: body, username?

    return null;
};

export default CommentForm;